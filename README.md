# RealMote Hub – Firmware-Updates
Signierte Over-the-Air-Firmware für den RealMote IR-Hub (ESP32-S3).
`version.json` ist das Update-Manifest (in der Hub-Firmware als UPDATE_CHECK_URL hinterlegt).
Nur mit dem privaten ECDSA-Schlüssel signierte Builds akzeptiert der Hub.
